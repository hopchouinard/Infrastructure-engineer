# F1.9: Integration Testing

## Objective

Create a comprehensive integration test suite that validates the complete documentation crawler workflow, testing all components working together with both mocked HTTP responses and optional live endpoint testing.

## Prerequisites

- F1.1 (Project Setup) completed
- F1.2 (Fetcher) completed
- F1.3 (Help Center Adapter) completed
- F1.4 (Guides Adapter) ~~completed~~ *DEFERRED - guides tests marked as skip*
- F1.5 (HTML Converter) completed
- F1.6 (PDF Converter) completed
- F1.7 (State Management) completed
- F1.8 (CLI Interface) completed

## Scope

### In Scope

- End-to-end workflow testing with mocked HTTP
- Component integration validation
- CLI command integration tests
- State persistence across operations
- Error propagation and recovery testing
- Optional live endpoint smoke tests (behind flag)
- Test fixtures for realistic scenarios

### Out of Scope

- Performance benchmarking
- Load testing
- Security testing
- Production deployment testing

## Implementation Requirements

### 1. Test Fixtures Module

**File: `tools/doc-crawler/tests/fixtures/__init__.py`**

```python
"""Shared test fixtures for integration testing."""

from .html_fixtures import (
    SAMPLE_HELP_CENTER_INDEX,
    SAMPLE_HELP_CENTER_CATEGORY,
    SAMPLE_HELP_CENTER_ARTICLE,
    SAMPLE_ARTICLE_CONTENT,
)
from .pdf_fixtures import SAMPLE_PDF_BYTES, create_test_pdf
from .guides_fixtures import SAMPLE_GUIDES_INDEX, SAMPLE_GUIDE_PDF_LIST

__all__ = [
    "SAMPLE_HELP_CENTER_INDEX",
    "SAMPLE_HELP_CENTER_CATEGORY",
    "SAMPLE_HELP_CENTER_ARTICLE",
    "SAMPLE_ARTICLE_CONTENT",
    "SAMPLE_PDF_BYTES",
    "create_test_pdf",
    "SAMPLE_GUIDES_INDEX",
    "SAMPLE_GUIDE_PDF_LIST",
]
```

**File: `tools/doc-crawler/tests/fixtures/html_fixtures.py`**

```python
"""HTML fixtures for Help Center testing."""

SAMPLE_HELP_CENTER_INDEX = """
<!DOCTYPE html>
<html>
<head><title>UniFi Help Center</title></head>
<body>
    <nav class="breadcrumb">Home</nav>
    <main>
        <div class="category-list">
            <a href="/hc/en-us/categories/123-unifi-network" class="category">
                <h2>UniFi Network</h2>
                <p>Network management documentation</p>
            </a>
            <a href="/hc/en-us/categories/456-unifi-protect" class="category">
                <h2>UniFi Protect</h2>
                <p>Video surveillance documentation</p>
            </a>
        </div>
    </main>
    <footer>Copyright 2024</footer>
</body>
</html>
"""

SAMPLE_HELP_CENTER_CATEGORY = """
<!DOCTYPE html>
<html>
<head><title>UniFi Network - Help Center</title></head>
<body>
    <nav class="breadcrumb">
        <a href="/hc/en-us">Home</a> > UniFi Network
    </nav>
    <main>
        <h1>UniFi Network</h1>
        <div class="section-list">
            <a href="/hc/en-us/sections/789-vlans" class="section">
                <h3>VLANs</h3>
                <p>VLAN configuration guides</p>
            </a>
            <a href="/hc/en-us/sections/101-firewall" class="section">
                <h3>Firewall</h3>
                <p>Firewall rule documentation</p>
            </a>
        </div>
        <div class="article-list">
            <a href="/hc/en-us/articles/111-getting-started" class="article">
                <h4>Getting Started with UniFi Network</h4>
            </a>
        </div>
    </main>
</body>
</html>
"""

SAMPLE_HELP_CENTER_ARTICLE = """
<!DOCTYPE html>
<html>
<head><title>Creating VLANs - Help Center</title></head>
<body>
    <nav class="breadcrumb">
        <a href="/hc/en-us">Home</a> >
        <a href="/hc/en-us/categories/123-unifi-network">UniFi Network</a> >
        <a href="/hc/en-us/sections/789-vlans">VLANs</a>
    </nav>
    <article>
        <h1>Creating VLANs in UniFi Network</h1>
        <div class="article-meta">
            <span class="last-updated">Updated: 2024-01-15</span>
        </div>
        <div class="article-body">
            <p>This guide explains how to create VLANs in UniFi Network.</p>

            <h2>Prerequisites</h2>
            <ul>
                <li>UniFi Network Application 8.0+</li>
                <li>UniFi managed switch</li>
            </ul>

            <h2>Steps</h2>
            <ol>
                <li>Navigate to Settings > Networks</li>
                <li>Click "Create New Network"</li>
                <li>Select VLAN-only option</li>
                <li>Enter VLAN ID (2-4094)</li>
                <li>Configure DHCP settings</li>
                <li>Apply changes</li>
            </ol>

            <h2>Example Configuration</h2>
            <pre><code>
VLAN ID: 100
Name: IoT Network
DHCP: Enabled
Range: 192.168.100.0/24
            </code></pre>

            <div class="note">
                <strong>Note:</strong> VLAN 1 is reserved for the default network.
            </div>
        </div>
    </article>
</body>
</html>
"""

SAMPLE_ARTICLE_CONTENT = """
<div class="article-body">
    <p>Simple article content for testing.</p>
    <h2>Section One</h2>
    <p>Some text with <strong>bold</strong> and <em>italic</em>.</p>
    <ul>
        <li>Item one</li>
        <li>Item two</li>
    </ul>
    <pre><code class="language-bash">echo "Hello World"</code></pre>
</div>
"""
```

**File: `tools/doc-crawler/tests/fixtures/guides_fixtures.py`**

```python
"""Fixtures for Guides adapter testing."""

SAMPLE_GUIDES_INDEX = """
<!DOCTYPE html>
<html>
<head><title>Index of /guides/UniFi/</title></head>
<body>
    <h1>Index of /guides/UniFi/</h1>
    <hr>
    <pre>
<a href="../">../</a>
<a href="UniFi_Network/">UniFi_Network/</a>                                   2024-01-10 12:00    -
<a href="UniFi_Protect/">UniFi_Protect/</a>                                    2024-01-08 10:30    -
<a href="README.txt">README.txt</a>                                         2024-01-01 09:00   1024
    </pre>
    <hr>
</body>
</html>
"""

SAMPLE_GUIDE_PDF_LIST = """
<!DOCTYPE html>
<html>
<head><title>Index of /guides/UniFi/UniFi_Network/</title></head>
<body>
    <h1>Index of /guides/UniFi/UniFi_Network/</h1>
    <hr>
    <pre>
<a href="../">../</a>
<a href="UDM_Pro_QSG.pdf">UDM_Pro_QSG.pdf</a>                                   2024-01-10 12:00  2457600
<a href="USW_Lite_8_QSG.pdf">USW_Lite_8_QSG.pdf</a>                                2024-01-09 11:00  1843200
<a href="UniFi_Network_Guide_v8.pdf">UniFi_Network_Guide_v8.pdf</a>                        2024-01-05 08:00  5242880
    </pre>
    <hr>
</body>
</html>
"""
```

**File: `tools/doc-crawler/tests/fixtures/pdf_fixtures.py`**

```python
"""PDF fixtures for testing."""

from io import BytesIO

# Minimal valid PDF bytes for testing
SAMPLE_PDF_BYTES = (
    b"%PDF-1.4\n"
    b"1 0 obj\n<< /Type /Catalog /Pages 2 0 R >>\nendobj\n"
    b"2 0 obj\n<< /Type /Pages /Kids [3 0 R] /Count 1 >>\nendobj\n"
    b"3 0 obj\n<< /Type /Page /Parent 2 0 R /MediaBox [0 0 612 792] "
    b"/Contents 4 0 R >>\nendobj\n"
    b"4 0 obj\n<< /Length 44 >>\nstream\nBT /F1 12 Tf 100 700 Td "
    b"(Test PDF Content) Tj ET\nendstream\nendobj\n"
    b"xref\n0 5\n0000000000 65535 f \n0000000009 00000 n \n"
    b"0000000058 00000 n \n0000000115 00000 n \n0000000214 00000 n \n"
    b"trailer\n<< /Size 5 /Root 1 0 R >>\nstartxref\n307\n%%EOF"
)


def create_test_pdf(title: str = "Test Document", content: str = "Test content") -> bytes:
    """
    Create a simple test PDF with custom content.

    For more complex PDFs, we use pypdf to generate them programmatically.

    Args:
        title: Document title
        content: Text content for the PDF

    Returns:
        PDF bytes
    """
    try:
        from pypdf import PdfWriter
        from pypdf.generic import (
            ArrayObject,
            DictionaryObject,
            NameObject,
            NumberObject,
            TextStringObject,
            ContentStream,
        )

        writer = PdfWriter()

        # Add a page with content
        page = writer.add_blank_page(width=612, height=792)

        # Add metadata
        writer.add_metadata({
            "/Title": title,
            "/Author": "Test Author",
            "/Subject": "Integration Test",
        })

        # Write to bytes
        output = BytesIO()
        writer.write(output)
        return output.getvalue()

    except ImportError:
        # Fallback to minimal PDF
        return SAMPLE_PDF_BYTES
```

### 2. Integration Test Suite

**File: `tools/doc-crawler/tests/integration/__init__.py`**

```python
"""Integration tests for the documentation crawler."""
```

**File: `tools/doc-crawler/tests/integration/test_full_workflow.py`**

```python
"""
Full workflow integration tests.

Tests the complete crawl -> convert -> save workflow with mocked HTTP.
"""

import json
import pytest
import tempfile
from pathlib import Path
from unittest.mock import AsyncMock, patch

import respx
from httpx import Response

from doc_crawler.config import CrawlerConfig
from doc_crawler.crawler import Crawler
from doc_crawler.fetcher import Fetcher
from doc_crawler.adapters.help_center import HelpCenterAdapter
from doc_crawler.adapters.guides import GuidesAdapter
from doc_crawler.converters.html import HTMLConverter
from doc_crawler.converters.pdf import PDFConverter
from doc_crawler.state import StateManager

from tests.fixtures import (
    SAMPLE_HELP_CENTER_INDEX,
    SAMPLE_HELP_CENTER_CATEGORY,
    SAMPLE_HELP_CENTER_ARTICLE,
    SAMPLE_GUIDES_INDEX,
    SAMPLE_GUIDE_PDF_LIST,
    SAMPLE_PDF_BYTES,
)


@pytest.fixture
def temp_output_dir():
    """Create a temporary directory for test output."""
    with tempfile.TemporaryDirectory() as tmpdir:
        yield Path(tmpdir)


@pytest.fixture
def test_config(temp_output_dir):
    """Create test configuration."""
    from doc_crawler.config import RateLimitConfig, SourcesConfig, HelpCenterConfig, GuidesConfig

    return CrawlerConfig(
        output_dir=temp_output_dir,
        rate_limit=RateLimitConfig(
            requests_per_second=100.0,  # Fast for tests
            respect_robots_txt=False,
        ),
        sources=SourcesConfig(
            help_center=HelpCenterConfig(
                enabled=True,
                base_url="https://help.ui.com/hc/en-us",
            ),
            guides=GuidesConfig(
                enabled=False,  # Guides adapter is deferred
            ),
        ),
    )


@pytest.fixture
def crawler(test_config):
    """Create a configured crawler instance."""
    return Crawler(test_config)


class TestHelpCenterWorkflow:
    """Test complete Help Center crawl workflow."""

    @respx.mock
    @pytest.mark.asyncio
    async def test_crawl_help_center_single_article(self, crawler, temp_output_dir):
        """Test crawling a single Help Center article."""
        # Mock the HTTP responses
        respx.get("https://help.ui.com/hc/en-us").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_INDEX)
        )
        respx.get("https://help.ui.com/hc/en-us/categories/123-unifi-network").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_CATEGORY)
        )
        respx.get("https://help.ui.com/hc/en-us/articles/111-getting-started").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_ARTICLE)
        )
        respx.get("https://help.ui.com/robots.txt").mock(
            return_value=Response(200, text="User-agent: *\nAllow: /")
        )

        # Run the crawl
        stats = await crawler.crawl_help_center(max_articles=1)

        # Verify results
        assert stats.articles_processed >= 1
        assert stats.errors == 0

        # Check output files exist
        help_center_dir = temp_output_dir / "help-center"
        assert help_center_dir.exists()

        # Find markdown files
        md_files = list(help_center_dir.rglob("*.md"))
        assert len(md_files) >= 1

        # Verify content
        content = md_files[0].read_text()
        assert "VLAN" in content or "Getting Started" in content

    @respx.mock
    @pytest.mark.asyncio
    async def test_help_center_state_persistence(self, crawler, temp_output_dir):
        """Test that state is persisted between crawl runs."""
        # Mock responses
        respx.get("https://help.ui.com/hc/en-us").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_INDEX)
        )
        respx.get("https://help.ui.com/hc/en-us/categories/123-unifi-network").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_CATEGORY)
        )
        respx.get("https://help.ui.com/hc/en-us/articles/111-getting-started").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_ARTICLE)
        )
        respx.get("https://help.ui.com/robots.txt").mock(
            return_value=Response(200, text="User-agent: *\nAllow: /")
        )

        # First crawl
        await crawler.crawl_help_center(max_articles=1)

        # Check state file exists
        state_file = temp_output_dir / ".crawler-state.json"
        assert state_file.exists()

        # Load and verify state
        state_data = json.loads(state_file.read_text())
        assert "help_center" in state_data
        assert len(state_data["help_center"]["processed_urls"]) > 0

    @respx.mock
    @pytest.mark.asyncio
    async def test_help_center_incremental_crawl(self, crawler, temp_output_dir):
        """Test incremental crawl skips already processed articles."""
        # Setup mocks
        respx.get("https://help.ui.com/hc/en-us").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_INDEX)
        )
        respx.get("https://help.ui.com/hc/en-us/categories/123-unifi-network").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_CATEGORY)
        )
        respx.get("https://help.ui.com/hc/en-us/articles/111-getting-started").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_ARTICLE)
        )
        respx.get("https://help.ui.com/robots.txt").mock(
            return_value=Response(200, text="User-agent: *\nAllow: /")
        )

        # First crawl
        stats1 = await crawler.crawl_help_center(max_articles=1)
        first_processed = stats1.articles_processed

        # Second crawl should skip already processed
        stats2 = await crawler.crawl_help_center(max_articles=1)

        # Second crawl should process fewer (or same if no new content)
        assert stats2.skipped >= first_processed or stats2.articles_processed == 0


@pytest.mark.skip(reason="F1.4 Guides Adapter is deferred to later phase")
class TestGuidesWorkflow:
    """Test complete Guides crawl workflow."""

    @respx.mock
    @pytest.mark.asyncio
    async def test_crawl_guides_pdf(self, crawler, temp_output_dir):
        """Test crawling and converting a PDF guide."""
        # Mock responses
        respx.get("https://dl.ubnt.com/guides/UniFi/").mock(
            return_value=Response(200, text=SAMPLE_GUIDES_INDEX)
        )
        respx.get("https://dl.ubnt.com/guides/UniFi/UniFi_Network/").mock(
            return_value=Response(200, text=SAMPLE_GUIDE_PDF_LIST)
        )
        respx.get("https://dl.ubnt.com/guides/UniFi/UniFi_Network/UDM_Pro_QSG.pdf").mock(
            return_value=Response(200, content=SAMPLE_PDF_BYTES)
        )
        respx.get("https://dl.ubnt.com/robots.txt").mock(
            return_value=Response(200, text="User-agent: *\nAllow: /")
        )

        # Run crawl
        stats = await crawler.crawl_guides(max_files=1)

        # Verify results
        assert stats.files_processed >= 1
        assert stats.errors == 0

        # Check output
        guides_dir = temp_output_dir / "guides"
        assert guides_dir.exists()

        # Should have markdown output
        md_files = list(guides_dir.rglob("*.md"))
        assert len(md_files) >= 1


@pytest.mark.skip(reason="F1.4 Guides Adapter is deferred - combined workflow tests pending")
class TestCombinedWorkflow:
    """Test combined Help Center + Guides workflow."""

    @respx.mock
    @pytest.mark.asyncio
    async def test_crawl_all(self, crawler, temp_output_dir):
        """Test crawling both sources."""
        # Mock Help Center
        respx.get("https://help.ui.com/hc/en-us").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_INDEX)
        )
        respx.get("https://help.ui.com/hc/en-us/categories/123-unifi-network").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_CATEGORY)
        )
        respx.get("https://help.ui.com/hc/en-us/articles/111-getting-started").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_ARTICLE)
        )
        respx.get("https://help.ui.com/robots.txt").mock(
            return_value=Response(200, text="User-agent: *\nAllow: /")
        )

        # Mock Guides
        respx.get("https://dl.ubnt.com/guides/UniFi/").mock(
            return_value=Response(200, text=SAMPLE_GUIDES_INDEX)
        )
        respx.get("https://dl.ubnt.com/guides/UniFi/UniFi_Network/").mock(
            return_value=Response(200, text=SAMPLE_GUIDE_PDF_LIST)
        )
        respx.get("https://dl.ubnt.com/guides/UniFi/UniFi_Network/UDM_Pro_QSG.pdf").mock(
            return_value=Response(200, content=SAMPLE_PDF_BYTES)
        )
        respx.get("https://dl.ubnt.com/robots.txt").mock(
            return_value=Response(200, text="User-agent: *\nAllow: /")
        )

        # Run combined crawl
        stats = await crawler.crawl_all(
            max_help_center_articles=1,
            max_guide_files=1
        )

        # Verify both sources processed
        assert stats.help_center.articles_processed >= 1
        assert stats.guides.files_processed >= 1

        # Verify directory structure
        assert (temp_output_dir / "help-center").exists()
        assert (temp_output_dir / "guides").exists()


class TestErrorHandling:
    """Test error handling and recovery."""

    @respx.mock
    @pytest.mark.asyncio
    async def test_http_error_recovery(self, crawler, temp_output_dir):
        """Test that crawler continues after HTTP errors."""
        # First request fails, second succeeds
        respx.get("https://help.ui.com/hc/en-us").mock(
            side_effect=[
                Response(500, text="Server Error"),
                Response(200, text=SAMPLE_HELP_CENTER_INDEX),
            ]
        )
        respx.get("https://help.ui.com/robots.txt").mock(
            return_value=Response(200, text="User-agent: *\nAllow: /")
        )

        # Should eventually succeed with retries
        # (This depends on retry configuration)
        try:
            await crawler.crawl_help_center(max_articles=1)
        except Exception:
            pass  # Expected if max retries exceeded

        # Crawler should not crash

    @respx.mock
    @pytest.mark.asyncio
    async def test_partial_failure_continues(self, crawler, temp_output_dir):
        """Test that crawler continues when some articles fail."""
        respx.get("https://help.ui.com/hc/en-us").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_INDEX)
        )
        respx.get("https://help.ui.com/hc/en-us/categories/123-unifi-network").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_CATEGORY)
        )
        # Article returns 404
        respx.get("https://help.ui.com/hc/en-us/articles/111-getting-started").mock(
            return_value=Response(404, text="Not Found")
        )
        respx.get("https://help.ui.com/robots.txt").mock(
            return_value=Response(200, text="User-agent: *\nAllow: /")
        )

        stats = await crawler.crawl_help_center(max_articles=10)

        # Should have recorded the error
        assert stats.errors >= 1

    @respx.mock
    @pytest.mark.asyncio
    async def test_timeout_handling(self, crawler, temp_output_dir):
        """Test timeout handling."""
        import httpx

        respx.get("https://help.ui.com/hc/en-us").mock(
            side_effect=httpx.TimeoutException("Timeout")
        )
        respx.get("https://help.ui.com/robots.txt").mock(
            return_value=Response(200, text="User-agent: *\nAllow: /")
        )

        stats = await crawler.crawl_help_center(max_articles=1)

        # Should have recorded error
        assert stats.errors >= 1
```

**File: `tools/doc-crawler/tests/integration/test_cli_integration.py`**

```python
"""
CLI integration tests.

Tests CLI commands with the full crawler stack.
"""

import json
import pytest
import tempfile
from pathlib import Path
from unittest.mock import patch, AsyncMock

from click.testing import CliRunner
import respx
from httpx import Response

from doc_crawler.cli import cli

from tests.fixtures import (
    SAMPLE_HELP_CENTER_INDEX,
    SAMPLE_HELP_CENTER_CATEGORY,
    SAMPLE_HELP_CENTER_ARTICLE,
    SAMPLE_GUIDES_INDEX,
    SAMPLE_GUIDE_PDF_LIST,
    SAMPLE_PDF_BYTES,
)


@pytest.fixture
def runner():
    """Create a CLI test runner."""
    return CliRunner()


@pytest.fixture
def temp_dir():
    """Create a temporary directory."""
    with tempfile.TemporaryDirectory() as tmpdir:
        yield Path(tmpdir)


class TestCrawlCommand:
    """Test the crawl CLI command."""

    @respx.mock
    def test_crawl_help_center(self, runner, temp_dir):
        """Test crawl command for Help Center."""
        # Setup mocks
        respx.get("https://help.ui.com/hc/en-us").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_INDEX)
        )
        respx.get("https://help.ui.com/hc/en-us/categories/123-unifi-network").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_CATEGORY)
        )
        respx.get("https://help.ui.com/hc/en-us/articles/111-getting-started").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_ARTICLE)
        )
        respx.get("https://help.ui.com/robots.txt").mock(
            return_value=Response(200, text="User-agent: *\nAllow: /")
        )

        result = runner.invoke(cli, [
            "crawl",
            "--source", "help-center",
            "--output", str(temp_dir),
            "--max-items", "1",
        ])

        assert result.exit_code == 0
        assert "Crawl completed" in result.output or "processed" in result.output.lower()

    @respx.mock
    def test_crawl_guides(self, runner, temp_dir):
        """Test crawl command for Guides."""
        respx.get("https://dl.ubnt.com/guides/UniFi/").mock(
            return_value=Response(200, text=SAMPLE_GUIDES_INDEX)
        )
        respx.get("https://dl.ubnt.com/guides/UniFi/UniFi_Network/").mock(
            return_value=Response(200, text=SAMPLE_GUIDE_PDF_LIST)
        )
        respx.get("https://dl.ubnt.com/guides/UniFi/UniFi_Network/UDM_Pro_QSG.pdf").mock(
            return_value=Response(200, content=SAMPLE_PDF_BYTES)
        )
        respx.get("https://dl.ubnt.com/robots.txt").mock(
            return_value=Response(200, text="User-agent: *\nAllow: /")
        )

        result = runner.invoke(cli, [
            "crawl",
            "--source", "guides",
            "--output", str(temp_dir),
            "--max-items", "1",
        ])

        assert result.exit_code == 0

    @respx.mock
    def test_crawl_all_sources(self, runner, temp_dir):
        """Test crawling all sources."""
        # Mock both sources
        respx.get("https://help.ui.com/hc/en-us").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_INDEX)
        )
        respx.get("https://help.ui.com/hc/en-us/categories/123-unifi-network").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_CATEGORY)
        )
        respx.get("https://help.ui.com/hc/en-us/articles/111-getting-started").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_ARTICLE)
        )
        respx.get("https://help.ui.com/robots.txt").mock(
            return_value=Response(200, text="User-agent: *\nAllow: /")
        )
        respx.get("https://dl.ubnt.com/guides/UniFi/").mock(
            return_value=Response(200, text=SAMPLE_GUIDES_INDEX)
        )
        respx.get("https://dl.ubnt.com/guides/UniFi/UniFi_Network/").mock(
            return_value=Response(200, text=SAMPLE_GUIDE_PDF_LIST)
        )
        respx.get("https://dl.ubnt.com/guides/UniFi/UniFi_Network/UDM_Pro_QSG.pdf").mock(
            return_value=Response(200, content=SAMPLE_PDF_BYTES)
        )
        respx.get("https://dl.ubnt.com/robots.txt").mock(
            return_value=Response(200, text="User-agent: *\nAllow: /")
        )

        result = runner.invoke(cli, [
            "crawl",
            "--source", "all",
            "--output", str(temp_dir),
            "--max-items", "1",
        ])

        assert result.exit_code == 0

    def test_crawl_invalid_source(self, runner, temp_dir):
        """Test error handling for invalid source."""
        result = runner.invoke(cli, [
            "crawl",
            "--source", "invalid",
            "--output", str(temp_dir),
        ])

        assert result.exit_code != 0


class TestStatusCommand:
    """Test the status CLI command."""

    def test_status_no_state(self, runner, temp_dir):
        """Test status when no crawl has been done."""
        result = runner.invoke(cli, [
            "status",
            "--output", str(temp_dir),
        ])

        # Should work but show no data
        assert result.exit_code == 0
        assert "No crawl state found" in result.output or "never" in result.output.lower()

    @respx.mock
    def test_status_after_crawl(self, runner, temp_dir):
        """Test status after a successful crawl."""
        # Do a crawl first
        respx.get("https://help.ui.com/hc/en-us").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_INDEX)
        )
        respx.get("https://help.ui.com/hc/en-us/categories/123-unifi-network").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_CATEGORY)
        )
        respx.get("https://help.ui.com/hc/en-us/articles/111-getting-started").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_ARTICLE)
        )
        respx.get("https://help.ui.com/robots.txt").mock(
            return_value=Response(200, text="User-agent: *\nAllow: /")
        )

        runner.invoke(cli, [
            "crawl",
            "--source", "help-center",
            "--output", str(temp_dir),
            "--max-items", "1",
        ])

        # Now check status
        result = runner.invoke(cli, [
            "status",
            "--output", str(temp_dir),
        ])

        assert result.exit_code == 0


class TestVerifyCommand:
    """Test the verify CLI command."""

    def test_verify_empty_output(self, runner, temp_dir):
        """Test verify on empty output directory."""
        result = runner.invoke(cli, [
            "verify",
            "--output", str(temp_dir),
        ])

        assert result.exit_code == 0
        assert "0" in result.output or "No files" in result.output

    @respx.mock
    def test_verify_after_crawl(self, runner, temp_dir):
        """Test verify after a successful crawl."""
        # Do a crawl first
        respx.get("https://help.ui.com/hc/en-us").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_INDEX)
        )
        respx.get("https://help.ui.com/hc/en-us/categories/123-unifi-network").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_CATEGORY)
        )
        respx.get("https://help.ui.com/hc/en-us/articles/111-getting-started").mock(
            return_value=Response(200, text=SAMPLE_HELP_CENTER_ARTICLE)
        )
        respx.get("https://help.ui.com/robots.txt").mock(
            return_value=Response(200, text="User-agent: *\nAllow: /")
        )

        runner.invoke(cli, [
            "crawl",
            "--source", "help-center",
            "--output", str(temp_dir),
            "--max-items", "1",
        ])

        # Verify
        result = runner.invoke(cli, [
            "verify",
            "--output", str(temp_dir),
        ])

        assert result.exit_code == 0


class TestConfigOptions:
    """Test CLI configuration options."""

    def test_verbose_flag(self, runner, temp_dir):
        """Test verbose output flag."""
        result = runner.invoke(cli, [
            "--verbose",
            "status",
            "--output", str(temp_dir),
        ])

        assert result.exit_code == 0

    def test_config_file_option(self, runner, temp_dir):
        """Test using a config file."""
        # Create config file
        config_file = temp_dir / "config.json"
        config_file.write_text(json.dumps({
            "output_dir": str(temp_dir / "output"),
            "rate_limit_requests": 10,
        }))

        result = runner.invoke(cli, [
            "--config", str(config_file),
            "status",
        ])

        # Should accept config file
        assert result.exit_code == 0
```

### 3. Optional Live Tests

**File: `tools/doc-crawler/tests/integration/test_live.py`**

```python
"""
Live endpoint tests.

These tests hit real endpoints and are disabled by default.
Run with: pytest -m live --live-tests

WARNING: These tests make real HTTP requests. Use responsibly.
"""

import pytest
import tempfile
from pathlib import Path

from doc_crawler.config import CrawlerConfig
from doc_crawler.crawler import Crawler


# Mark all tests in this module as live tests
pytestmark = [
    pytest.mark.live,
    pytest.mark.skip(reason="Live tests disabled by default"),
]


def pytest_configure(config):
    """Register the live marker."""
    config.addinivalue_line(
        "markers", "live: mark test as requiring live network access"
    )


@pytest.fixture
def live_config():
    """Configuration for live tests."""
    from doc_crawler.config import RateLimitConfig, SourcesConfig, HelpCenterConfig, GuidesConfig

    with tempfile.TemporaryDirectory() as tmpdir:
        yield CrawlerConfig(
            output_dir=Path(tmpdir),
            rate_limit=RateLimitConfig(
                requests_per_second=0.5,  # Be very conservative (1 req per 2 seconds)
                respect_robots_txt=True,
            ),
            sources=SourcesConfig(
                help_center=HelpCenterConfig(enabled=True),
                guides=GuidesConfig(enabled=False),  # Deferred
            ),
        )


class TestLiveHelpCenter:
    """Live tests for Help Center."""

    @pytest.mark.asyncio
    async def test_help_center_index_accessible(self, live_config):
        """Verify Help Center index is accessible."""
        crawler = Crawler(live_config)

        async with crawler.fetcher as fetcher:
            response = await fetcher.get("https://help.ui.com/hc/en-us")
            assert response.status_code == 200
            assert "UniFi" in response.text or "Ubiquiti" in response.text

    @pytest.mark.asyncio
    async def test_crawl_single_help_article(self, live_config):
        """Test crawling a single real Help Center article."""
        crawler = Crawler(live_config)

        stats = await crawler.crawl_help_center(max_articles=1)

        # Should process at least one article without errors
        assert stats.articles_processed >= 1 or stats.skipped >= 1
        assert stats.errors == 0


@pytest.mark.skip(reason="F1.4 Guides Adapter is deferred to later phase")
class TestLiveGuides:
    """Live tests for Guides."""

    @pytest.mark.asyncio
    async def test_guides_index_accessible(self, live_config):
        """Verify Guides index is accessible."""
        crawler = Crawler(live_config)

        async with crawler.fetcher as fetcher:
            response = await fetcher.get("https://dl.ubnt.com/guides/UniFi/")
            assert response.status_code == 200
            # Should be a directory listing
            assert "Index of" in response.text or "href=" in response.text

    @pytest.mark.asyncio
    async def test_crawl_single_guide(self, live_config):
        """Test crawling a single real PDF guide."""
        crawler = Crawler(live_config)

        stats = await crawler.crawl_guides(max_files=1)

        # Should process at least one file without errors
        assert stats.files_processed >= 1 or stats.skipped >= 1
        assert stats.errors == 0


class TestLiveRobotsTxt:
    """Test robots.txt compliance with live sites."""

    @pytest.mark.asyncio
    async def test_help_center_robots(self, live_config):
        """Check Help Center robots.txt."""
        crawler = Crawler(live_config)

        async with crawler.fetcher as fetcher:
            response = await fetcher.get("https://help.ui.com/robots.txt")
            # Should either succeed or 404 (no robots.txt)
            assert response.status_code in [200, 404]

    @pytest.mark.asyncio
    async def test_guides_robots(self, live_config):
        """Check Guides robots.txt."""
        crawler = Crawler(live_config)

        async with crawler.fetcher as fetcher:
            response = await fetcher.get("https://dl.ubnt.com/robots.txt")
            assert response.status_code in [200, 404]
```

### 4. Pytest Configuration for Live Tests

**File: `tools/doc-crawler/tests/conftest.py`** (additions)

```python
"""
Pytest configuration and shared fixtures.
"""

import pytest


def pytest_addoption(parser):
    """Add custom command line options."""
    parser.addoption(
        "--live-tests",
        action="store_true",
        default=False,
        help="Run tests that require live network access",
    )


def pytest_configure(config):
    """Register custom markers."""
    config.addinivalue_line(
        "markers", "live: mark test as requiring live network access"
    )


def pytest_collection_modifyitems(config, items):
    """Skip live tests unless --live-tests is passed."""
    if config.getoption("--live-tests"):
        # --live-tests given, don't skip live tests
        return

    skip_live = pytest.mark.skip(reason="need --live-tests option to run")
    for item in items:
        if "live" in item.keywords:
            item.add_marker(skip_live)
```

### 5. Test Runner Script

**File: `tools/doc-crawler/scripts/run_integration_tests.sh`**

```bash
#!/bin/bash
# Run integration tests for the documentation crawler

set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_DIR="$(dirname "$SCRIPT_DIR")"

cd "$PROJECT_DIR"

echo "Running integration tests..."
echo "=============================="

# Run with coverage
python -m pytest tests/integration/ \
    -v \
    --tb=short \
    --cov=doc_crawler \
    --cov-report=term-missing \
    --cov-report=html:coverage_html \
    "$@"

echo ""
echo "Integration tests completed!"
echo "Coverage report: coverage_html/index.html"
```

**File: `tools/doc-crawler/scripts/run_live_tests.sh`**

```bash
#!/bin/bash
# Run live integration tests (makes real HTTP requests)

set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_DIR="$(dirname "$SCRIPT_DIR")"

cd "$PROJECT_DIR"

echo "=============================="
echo "WARNING: Running LIVE tests"
echo "This will make real HTTP requests to:"
echo "  - help.ui.com"
echo "  - dl.ubnt.com"
echo "=============================="
echo ""

read -p "Continue? (y/N) " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "Aborted."
    exit 1
fi

echo ""
echo "Running live tests..."
python -m pytest tests/integration/test_live.py \
    -v \
    --tb=short \
    --live-tests \
    "$@"
```

## Test Requirements

### Test Execution

```bash
# Run all integration tests (mocked)
pytest tests/integration/ -v

# Run with coverage
pytest tests/integration/ -v --cov=doc_crawler

# Run specific test class
pytest tests/integration/test_full_workflow.py::TestHelpCenterWorkflow -v

# Run live tests (requires network)
pytest tests/integration/test_live.py -v --live-tests
```

### Expected Results

1. **Mocked Tests**:
   - All pass without network access
   - Fast execution (< 30 seconds total)
   - 100% deterministic

2. **Live Tests**:
   - Pass when network available and sites responding
   - May fail if site structure changes
   - Respect rate limits

## Acceptance Criteria

1. **Workflow Tests**:
   - [ ] Full Help Center crawl workflow tested
   - [ ] Full Guides crawl workflow tested
   - [ ] Combined workflow tested
   - [ ] State persistence verified

2. **CLI Tests**:
   - [ ] All CLI commands tested
   - [ ] Error handling verified
   - [ ] Configuration options work

3. **Error Handling**:
   - [ ] HTTP errors handled gracefully
   - [ ] Partial failures don't crash crawler
   - [ ] Timeouts handled

4. **Live Tests**:
   - [ ] Live tests exist (disabled by default)
   - [ ] Can be enabled via flag
   - [ ] Respect rate limits

5. **Coverage**:
   - [ ] Integration tests achieve >80% code coverage
   - [ ] All major code paths exercised

## Files to Create/Modify

| File | Action | Purpose |
|------|--------|---------|
| `tests/fixtures/__init__.py` | Create | Fixture exports |
| `tests/fixtures/html_fixtures.py` | Create | HTML test data |
| `tests/fixtures/guides_fixtures.py` | Create | Guides test data |
| `tests/fixtures/pdf_fixtures.py` | Create | PDF test data |
| `tests/integration/__init__.py` | Create | Package marker |
| `tests/integration/test_full_workflow.py` | Create | Workflow tests |
| `tests/integration/test_cli_integration.py` | Create | CLI tests |
| `tests/integration/test_live.py` | Create | Live endpoint tests |
| `tests/conftest.py` | Modify | Add live test options |
| `scripts/run_integration_tests.sh` | Create | Test runner |
| `scripts/run_live_tests.sh` | Create | Live test runner |

## Implementation Notes

### Mocking Strategy

- Use `respx` for HTTP mocking (integrates with httpx)
- Mock at HTTP level, not function level
- Provide realistic HTML/PDF responses

### Test Isolation

- Each test uses fresh temp directory
- No shared state between tests
- Clean up after each test

### Live Test Safety

- Disabled by default
- Very conservative rate limits
- Minimal requests (1-2 per test)
- Warning before running

## Dependencies

- pytest >= 8.0.0
- pytest-asyncio >= 0.23.0
- pytest-cov >= 4.1.0
- respx >= 0.20.0

## Estimated Complexity

- **Lines of Code**: ~800
- **Files**: 11
- **Estimated Time**: 4-6 hours
- **Risk Level**: Low (testing existing code)
