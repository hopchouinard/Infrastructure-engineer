# Task F3.8: Change Documentation System

**Phase:** 1 (UniFi Infrastructure)
**Feature:** 3 (Claude Code Integration)
**Milestone:** F3.8 - Change Documentation System
**Status:** Not Started

---

## 1. Objective

Implement a mandatory documentation system for all infrastructure modifications. No task that modifies, updates, deletes, or changes infrastructure can be considered complete until the change has been properly documented in a format optimized for Claude Code consumption.

## 2. Prerequisites

- F3.1 (Project Setup) completed - directory structure exists
- F3.2 (Core Skills) completed - workflow patterns defined
- F3.4 (Hooks Configuration) completed - hook infrastructure exists
- F2 (UniFi MCP Server) functional - tools return rollback IDs

## 3. Scope

### In Scope

- Define semantic naming convention for change documents
- Create queryable index structure (`index.yaml`)
- Create change documentation template
- Implement PostToolUse hook for documentation workflow
- Add mandatory documentation rule to SAFETY.md
- Add documentation pattern to skill workflows
- Test documentation enforcement

### Out of Scope

- Full-text search across change documents
- Automatic document generation (Claude creates manually)
- External storage or backup of documentation
- Version control integration for documents

---

## 4. Background: AI-Optimized Documentation

### 4.1 Why Semantic Naming Matters

Traditional chronological naming (e.g., `CHG-2025-01-09-001.md`) is human-centric. An AI agent benefits from:

| Approach | AI Benefit |
|----------|------------|
| **Resource-first naming** | Glob patterns find related changes (`*living-room-tv*`) |
| **Semantic filenames** | Filename alone communicates content |
| **Structured index** | Programmatic queries without reading all files |
| **No dates in paths** | Temporal data in metadata, not identity |

### 4.2 Documentation as First-Class Requirement

Every infrastructure modification must be documented because:
1. **Context for future operations** - Claude can reference past decisions
2. **Audit trail** - What was done, why, and how to undo
3. **Pattern learning** - Similar operations can reference past examples
4. **Rollback support** - Links changes to rollback IDs

### 4.3 Documentation vs Audit Logging

| Aspect | Audit Log (F2.8) | Change Documentation (F3.8) |
|--------|------------------|----------------------------|
| **Format** | Structured JSON | Semantic Markdown |
| **Content** | What happened | Why it happened + context |
| **Created by** | Automatic (hook) | Claude (guided by pattern) |
| **Purpose** | Technical record | Knowledge for future queries |
| **Queryable by** | Parsing tools | Glob/Grep + Index |

---

## 5. Implementation Requirements

### 5.1 Directory Structure

Create the following structure for change documentation:

```
ai_docs/
└── changes/
    ├── index.yaml              # Queryable index of all changes
    ├── templates/
    │   └── change-doc.md       # Template for new documentation
    └── docs/
        ├── .gitkeep
        └── [semantic-named-docs].md
```

### 5.2 Naming Convention

**Pattern:** `{primary-resource}-{operation}.md`

| Component | Description | Examples |
|-----------|-------------|----------|
| `primary-resource` | Main affected resource (slugified) | `living-room-tv`, `iot-media-vlan`, `camera-network` |
| `operation` | What was done | `isolation`, `creation`, `firewall-setup`, `blocking` |

**Examples:**
- `living-room-tv-isolation.md`
- `iot-media-vlan-creation.md`
- `camera-network-firewall-setup.md`
- `guest-wifi-24h-creation.md`
- `unknown-device-aa11bb22-blocking.md`

**Rules:**
- Lowercase with hyphens (no spaces, underscores, or special chars)
- Resource name first (what was affected)
- Operation second (what was done)
- No dates in filename (dates in metadata)
- Keep names concise but descriptive

### 5.3 Index Structure

**File: `ai_docs/changes/index.yaml`**

```yaml
# Change Documentation Index
# This file is the primary query interface for Claude Code
# Updated after each change documentation is created

---
version: "1.0"
last_updated: "2025-01-09T15:30:00Z"

# All documented changes
changes:
  - id: living-room-tv-isolation
    file: docs/living-room-tv-isolation.md
    timestamp: "2025-01-09T15:30:00Z"
    type: device_isolation
    summary: Isolated Samsung TV to dedicated IoT VLAN with NAS access
    resources:
      - type: device
        name: Living Room TV
        mac: "aa:bb:cc:dd:ee:01"
      - type: network
        name: IoT-Media
        vlan_id: 40
      - type: firewall_rule
        name: IoT-Media-to-NAS
    rollback_ids:
      - a1b2c3d4
      - e5f6g7h8
    tags:
      - isolation
      - iot
      - tv
      - vlan-40

  - id: camera-network-creation
    file: docs/camera-network-creation.md
    timestamp: "2025-01-08T10:00:00Z"
    type: network_creation
    summary: Created dedicated camera VLAN with strict isolation
    resources:
      - type: network
        name: Cameras
        vlan_id: 35
    rollback_ids:
      - x1y2z3w4
    tags:
      - cameras
      - security
      - vlan-35

# Query helpers - pre-computed for efficient lookups
by_resource:
  devices:
    living-room-tv:
      - living-room-tv-isolation
  networks:
    iot-media:
      - living-room-tv-isolation
    cameras:
      - camera-network-creation
  firewall_rules:
    iot-media-to-nas:
      - living-room-tv-isolation

by_type:
  device_isolation:
    - living-room-tv-isolation
  network_creation:
    - camera-network-creation

by_tag:
  isolation:
    - living-room-tv-isolation
  iot:
    - living-room-tv-isolation
  cameras:
    - camera-network-creation
  vlan-35:
    - camera-network-creation
  vlan-40:
    - living-room-tv-isolation

# Recent changes (last 10 for quick reference)
recent:
  - living-room-tv-isolation
  - camera-network-creation
```

### 5.4 Change Documentation Template

**File: `ai_docs/changes/templates/change-doc.md`**

```markdown
---
id: {resource}-{operation}
timestamp: {ISO-8601 timestamp}
type: {operation_type}
status: completed
requested_by: user
executed_by: claude-code

resources:
  - type: {device|network|firewall_rule|port}
    name: {resource_name}
    identifier: {id_or_mac}

rollback_ids:
  - {rollback_id_1}
  - {rollback_id_2}

related_changes:
  - {previous_change_id}

tags:
  - {tag1}
  - {tag2}
---

# Change: {Descriptive Title}

## Context

{Why was this change requested? What problem does it solve? Include user's original request or intent.}

## Pre-Change State

{What was the state before the change? Relevant current configuration.}

- **Device/Resource:** {name}
- **Previous Network/VLAN:** {previous_state}
- **Previous Configuration:** {relevant_config}

## Changes Made

### 1. {First Change}

- **Tool Used:** `{mcp_tool_name}`
- **Parameters:**
  ```json
  {tool_parameters}
  ```
- **Result:** {success/failure + details}
- **Rollback ID:** `{rollback_id}`

### 2. {Second Change}

{...repeat for each change...}

## Post-Change State

{What is the state after the change? Verification results.}

- **Device/Resource:** {name}
- **New Network/VLAN:** {new_state}
- **New IP Address:** {if applicable}
- **Verification:** {how was success confirmed}

## Rationale

{Why was this specific approach chosen? What alternatives were considered?}

## Impact Assessment

- **Affected Devices:** {list of affected devices}
- **Downtime:** {expected/actual downtime}
- **Risk Level:** {LOW/MEDIUM/HIGH}

## Rollback Procedure

To undo these changes, execute in reverse order:

```
/infra-undo --id {last_rollback_id}
/infra-undo --id {second_to_last_rollback_id}
...
```

Or use the MCP tools directly:

```
unifi_undo(rollback_id="{rollback_id}")
```

**Note:** Rollback IDs expire after {expiry_time} minutes.

## Related Documentation

- Previous state: {link if applicable}
- Similar changes: {links to related change docs}
- Vendor documentation: {relevant UniFi docs}

## Lessons Learned

{Optional: Any issues encountered, things to do differently next time}
```

### 5.5 Post-Change Documentation Hook

**File: `.claude/hooks/post-change-document.sh`**

This hook triggers after configuration changes and reminds Claude to create documentation.

```bash
#!/usr/bin/env bash
#
# Post-change documentation reminder hook
# Triggers after configuration changes to prompt documentation workflow
#
# This hook does NOT block - it outputs a reminder message
#

set -euo pipefail

# Read input
INPUT=$(cat)

# Parse JSON
TOOL_NAME=$(echo "$INPUT" | jq -r '.tool_name // "unknown"')
SUCCESS=$(echo "$INPUT" | jq -r '.result.success // false')
ROLLBACK_ID=$(echo "$INPUT" | jq -r '.result.rollback_id // "none"')

# Only prompt for successful configuration changes
if [[ "$SUCCESS" != "true" ]]; then
    exit 0
fi

# Extract operation type from tool name
OPERATION=""
case "$TOOL_NAME" in
    *create_network*)
        OPERATION="network creation"
        ;;
    *delete_network*)
        OPERATION="network deletion"
        ;;
    *create_firewall_rule*)
        OPERATION="firewall rule creation"
        ;;
    *delete_firewall_rule*)
        OPERATION="firewall rule deletion"
        ;;
    *set_port_vlan*)
        OPERATION="port VLAN configuration"
        ;;
    *block_client*)
        OPERATION="client blocking"
        ;;
    *unblock_client*)
        OPERATION="client unblocking"
        ;;
    *)
        # Not a tracked configuration change
        exit 0
        ;;
esac

# Output documentation reminder
cat << EOF

---
📋 **DOCUMENTATION REQUIRED**

A $OPERATION has been completed successfully.
Rollback ID: $ROLLBACK_ID

Before this task can be considered complete, you must:

1. Create a change document in \`ai_docs/changes/docs/\`
   - Use semantic naming: \`{resource}-{operation}.md\`
   - Follow template: \`ai_docs/changes/templates/change-doc.md\`

2. Update the index: \`ai_docs/changes/index.yaml\`
   - Add entry to \`changes\` list
   - Update \`by_resource\`, \`by_type\`, \`by_tag\` sections
   - Add to \`recent\` list

This documentation requirement is MANDATORY per project safety rules.
---

EOF
```

### 5.6 SAFETY.md Verification

Verify the following section exists in `.claude/skills/unifi-infra/SAFETY.md` (added by F3.2):

```markdown
## Mandatory Documentation Rule

**CRITICAL:** No infrastructure modification is complete until documented.

### Requirement

Before marking ANY task complete that involves:
- Creating, updating, or deleting networks/VLANs
- Creating, updating, or deleting firewall rules
- Changing port configurations
- Blocking or unblocking clients
- Any operation that returns a `rollback_id`

You MUST:

1. **Create a change document** in `ai_docs/changes/docs/`
   - Filename: `{primary-resource}-{operation}.md`
   - Use template from `ai_docs/changes/templates/change-doc.md`
   - Include all context useful for future operations

2. **Update the index** in `ai_docs/changes/index.yaml`
   - Add entry to `changes` list with full metadata
   - Update `by_resource` mappings
   - Update `by_type` mappings
   - Update `by_tag` mappings
   - Add to `recent` list (keep last 10)

3. **Link rollback IDs** in the documentation
   - All rollback IDs from the operation
   - Clear rollback procedure

### What to Document

Include everything that would help Claude Code understand this change in the future:

| Include | Example |
|---------|---------|
| **Why** the change was made | "User wanted to isolate TV for security" |
| **What** resources were affected | Device MAC, VLAN ID, rule names |
| **How** it was accomplished | Tools used, parameters, sequence |
| **Pre/post state** | Before: Default VLAN, After: VLAN 40 |
| **Rollback procedure** | Specific commands to undo |
| **Lessons learned** | Issues encountered, alternatives considered |

### Enforcement

The `post-change-document.sh` hook will remind you after each configuration change. Do not proceed to the next task until documentation is complete.

### Index Query Patterns

When Claude needs to find relevant history:

```yaml
# Find all changes to a device
by_resource.devices.{device-name}

# Find all network creations
by_type.network_creation

# Find all IoT-related changes
by_tag.iot

# Get recent changes
recent
```
```

### 5.7 Skill Pattern Verification

Verify the following section exists in `.claude/skills/unifi-infra/PATTERNS.md` (added by F3.2):

```markdown
## Documentation Pattern

After ANY configuration change, follow this documentation workflow:

### Step 1: Gather Change Details

Collect from the completed operation:
- All rollback IDs returned
- Resources created/modified/deleted
- Tool parameters used
- Pre and post state

### Step 2: Create Change Document

```bash
# Determine filename
RESOURCE="living-room-tv"  # Primary affected resource
OPERATION="isolation"       # What was done
FILENAME="${RESOURCE}-${OPERATION}.md"

# Create file
ai_docs/changes/docs/${FILENAME}
```

Use the template from `ai_docs/changes/templates/change-doc.md`.

### Step 3: Update Index

Read current `ai_docs/changes/index.yaml` and add:

1. New entry in `changes` list:
   ```yaml
   - id: living-room-tv-isolation
     file: docs/living-room-tv-isolation.md
     timestamp: "2025-01-09T15:30:00Z"
     type: device_isolation
     summary: Isolated TV to dedicated VLAN
     resources: [...]
     rollback_ids: [...]
     tags: [...]
   ```

2. Update `by_resource`:
   ```yaml
   by_resource:
     devices:
       living-room-tv:
         - living-room-tv-isolation
   ```

3. Update `by_type`:
   ```yaml
   by_type:
     device_isolation:
       - living-room-tv-isolation
   ```

4. Update `by_tag` for each tag

5. Add to `recent` (maintain last 10)

### Step 4: Confirm Documentation

Report to user:
```
✓ Change documented: ai_docs/changes/docs/living-room-tv-isolation.md
✓ Index updated with {n} new entries
✓ Rollback IDs preserved: [a1b2c3d4, e5f6g7h8]
```

### Documentation Naming Examples

| Scenario | Filename |
|----------|----------|
| Isolate TV to VLAN | `living-room-tv-isolation.md` |
| Create IoT network | `iot-network-creation.md` |
| Add firewall allow rule | `iot-to-nas-firewall-allow.md` |
| Block unknown device | `unknown-device-aabb-blocking.md` |
| Create guest WiFi | `guest-wifi-party-creation.md` |
```

---

## 6. Test Requirements

### 6.1 Documentation Creation Tests

**Test 1: Document Created After Isolation**
```
Action: Complete a device isolation workflow
Expected:
- Documentation reminder appears after each config tool
- Change document created in ai_docs/changes/docs/
- Filename follows semantic convention
- Document uses template structure
```

**Test 2: Index Updated Correctly**
```
Action: Create change documentation
Expected:
- Entry added to changes list
- by_resource updated
- by_type updated
- by_tag updated
- recent list updated (max 10 entries)
```

**Test 3: Index is Queryable**
```
Action: Query index for specific resource
Expected:
- Can find changes by device name
- Can find changes by operation type
- Can find changes by tag
- Can retrieve recent changes
```

### 6.2 Enforcement Tests

**Test 4: Reminder Hook Fires**
```
Action: Execute a configuration tool
Expected:
- Hook outputs documentation reminder
- Reminder includes rollback ID
- Reminder specifies required steps
```

**Test 5: Task Not Complete Without Documentation**
```
Action: Attempt to end task after config change without documenting
Expected:
- Claude references SAFETY.md requirement
- Claude creates documentation before completing
```

### 6.3 Query Tests

**Test 6: Find Related Changes**
```
Scenario: User asks "What was done to the TV before?"
Expected:
- Claude reads index.yaml
- Finds entries in by_resource.devices.{tv-name}
- Reads relevant change documents
- Reports history
```

**Test 7: Find Similar Operations**
```
Scenario: User asks to isolate another device
Expected:
- Claude queries by_type.device_isolation
- Reads previous isolation documentation
- Uses as reference for new operation
```

---

## 7. Acceptance Criteria

### 7.1 Functional Criteria

- [ ] Directory structure created (`ai_docs/changes/`)
- [ ] Index file structure defined and documented
- [ ] Template file created with all sections
- [ ] Post-change documentation hook implemented
- [ ] SAFETY.md updated with documentation rule
- [ ] PATTERNS.md updated with documentation pattern
- [ ] Naming convention clearly defined and documented

### 7.2 Integration Criteria

- [ ] Hook fires after all configuration tools
- [ ] Claude follows documentation pattern consistently
- [ ] Index is updated after each documented change
- [ ] Previous changes are queryable via index

### 7.3 Quality Criteria

- [ ] Semantic names are descriptive and consistent
- [ ] Index structure supports efficient queries
- [ ] Template captures all necessary information
- [ ] Documentation is optimized for AI consumption

---

## 8. Files to Create/Modify

| File | Action | Purpose |
|------|--------|---------|
| `ai_docs/changes/index.yaml` | Create | Queryable change index |
| `ai_docs/changes/templates/change-doc.md` | Create | Documentation template |
| `ai_docs/changes/docs/.gitkeep` | Create | Preserve directory |
| `.claude/hooks/post-change-document.sh` | Create | Documentation reminder |
| `.claude/skills/unifi-infra/SAFETY.md` | Verify | Documentation rule exists (added by F3.2) |
| `.claude/skills/unifi-infra/PATTERNS.md` | Verify | Documentation pattern exists (added by F3.2) |
| `.claude/settings.json` | Modify | Add hook configuration |

---

## 9. Implementation Notes

### 9.1 Index Maintenance

The index file (`index.yaml`) should be updated by Claude after each documentation:
- This is NOT automatic - Claude updates it as part of the documentation workflow
- The index is designed for efficient querying, not just storage
- The `by_*` sections are denormalized for fast lookups

### 9.2 Naming Conflicts

If a filename would conflict:
- Append a distinguishing suffix: `living-room-tv-isolation-2.md`
- Or use more specific naming: `living-room-tv-camera-vlan-isolation.md`
- Never reuse/overwrite existing documentation

### 9.3 Large Operations

For operations that create many resources:
- Single document for the overall operation
- List all resources in the document
- Use clear sections for each change
- Cross-reference related changes

### 9.4 Failed Operations

Document failed operations if:
- Partial changes were made
- Rollback was required
- The failure provides useful context for future attempts

---

## 10. Dependencies

### 10.1 Upstream Dependencies

- F3.1 (Project Setup) - Base directory structure
- F3.2 (Core Skills) - Skill files to modify
- F3.4 (Hooks Configuration) - Hook infrastructure
- F2.7 (Rollback System) - Rollback IDs to document

### 10.2 Downstream Dependencies

- Future operations reference documented changes
- SubAgents (F4) query documentation for context

---

## 11. Estimated Complexity

- **Files to Create**: 4
- **Files to Modify**: 3
- **Lines of Content**: ~400
- **Risk Level**: Low (documentation, not code execution)

---

*Task Version: 1.0*
*Last Updated: 2026-01-11*
